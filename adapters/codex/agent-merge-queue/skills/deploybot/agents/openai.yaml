interface:
  display_name: "DeployBot"
  short_description: "Inspect and operate the delivery pipeline"
  default_prompt: "Use $deploybot to show threads, pull requests, queue, CI, deployment, and any blocker or repair handoff."
dependencies:
  tools:
    - type: "mcp"
      value: "deploybot"
      description: "Durable GitHub merge queue operations"
