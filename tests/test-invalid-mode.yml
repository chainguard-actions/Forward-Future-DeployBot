name: Test invalid mode exits with error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        continue-on-error: true
        with:
          mode: invalid-mode
          config: tests/fixtures/mergequeue.toml

      - name: Assert action failed with invalid mode
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail with invalid mode, got '$OUTCOME'"
            exit 1
          fi
          echo "PASS: action correctly failed for invalid mode"
